#include "utils/containers/multiset_minus.h"

namespace FlexFlow {

template <typename T>
std::multiset<T> multiset_minus(std::multiset<T> const &,
                                std::multiset<T> const &);

} // namespace FlexFlow
